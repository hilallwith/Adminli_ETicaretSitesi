@model IEnumerable<Site.Entity.Yorum>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Product.OAD)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KullanıcıAd)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Icerik)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Product.OAD)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KullanıcıAd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Icerik)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.YorumID }) |
                @Html.ActionLink("Details", "Details", new { id=item.YorumID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.YorumID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
